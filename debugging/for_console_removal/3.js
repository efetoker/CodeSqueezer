// This script performs a simple task without logging
function multiply(a, b) {
  return a * b;
}

const factor1 = 7;
const factor2 = 6;
const product = multiply(factor1, factor2);

// No console output here, result might be used elsewhere
// For example: document.getElementById('result').innerText = product;;;
