function calculateTotal(a, b) {
    const result = a + b;
    // console.log(`Calculating ${a} + ${b} = ${result}`); // Maybe a temporary debug log
    return result;
}

const x = 10;
const y = 25;
const total = calculateTotal(x, y);

console.log(`The final total is: ${total}`); // Log the final result